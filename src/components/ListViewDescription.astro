---
import Text from "./Text.astro";

interface Props {
  class?: string;
}

const { class: className = "", ...attrs } = Astro.props;
---

<Text class={`text-lg md:text-xl font-medium leading-none text-stone-700 ${className}`} {...attrs}>
  <slot />
</Text>
