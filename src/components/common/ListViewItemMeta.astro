---
import { Paragraph } from "@components/base";

interface Props {
  class?: string;
}

const { class: className = "", ...attrs } = Astro.props;
---

<Paragraph class={`text-stone-500 ${className}`} {...attrs}>
  <slot />
</Paragraph>