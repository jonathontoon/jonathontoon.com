---
import { Paragraph } from "@components/base";

interface Props {
  class?: string;
}

const { class: className = "", ...attrs } = Astro.props;
---

<Paragraph class={`${className}`} {...attrs}>
  <slot />
</Paragraph>