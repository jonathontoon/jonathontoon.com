---
import { Paragraph } from "@components/base";

interface Props {
  class?: string;
}

const { class: className = "", ...attrs } = Astro.props;
---

<Paragraph class={`font-normal ${className}`} {...attrs}>
  <slot />
</Paragraph>