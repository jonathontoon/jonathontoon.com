---
import type { HTMLAttributes } from "astro/types";
import { UnorderedList } from "@components/base";

interface Props extends HTMLAttributes<"ul"> {
  class?: string;
}

const { class: className = "", ...attrs } = Astro.props;
---

<UnorderedList class={className} {...attrs}>
  <slot />
</UnorderedList>