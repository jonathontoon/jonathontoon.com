---
import { UnorderedList } from "@components/base";

interface Props {
  class?: string;
}

const { class: className = "", ...attrs } = Astro.props;
---

<UnorderedList class:list={["flex flex-col gap-0.5 sm:gap-1", className]} {...attrs}>
  <slot />
</UnorderedList>