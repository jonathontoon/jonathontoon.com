---
import type { HTMLAttributes } from "astro/types";

export interface LinkProps extends HTMLAttributes<"a"> {
  href?: string;
  class?: string;
}

const { href, class: classList = "", ...attrs } = Astro.props;
---

<a
  rel="prefectch"
  href={href}
  class:list={[
    "inline-block cursor-pointer px-6 py-2 text-center text-base font-semibold text-neutral-50 transition-colors duration-300 [:where(&)]:bg-neutral-800 [:where(&)]:hover:bg-neutral-900",
    classList
  ]}
  {...attrs}><slot /></a
>
