---
import type { HTMLAttributes } from "astro/types";

interface Props extends HTMLAttributes<"ul"> {
  class?: string;
}

const { class: className = "", ...attrs } = Astro.props;
---

<ul {...attrs} class:list={["flex", "flex-col", className]}>
  <slot />
</ul>
