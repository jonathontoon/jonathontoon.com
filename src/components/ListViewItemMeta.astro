---
import TextSmall from "./TextSmall.astro";

interface Props {
  class?: string;
}

const { class: className = "", ...attrs } = Astro.props;
---

<TextSmall class={`text-lg font-medium text-stone-500 dark:text-stone-400 leading-none ${className}`} {...attrs}>
  <slot />
</TextSmall>
