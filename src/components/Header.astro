---
import type { HTMLAttributes } from "astro/types";

interface Props extends HTMLAttributes<"header"> {
  class?: string;
}

const { class: className = "", ...attrs } = Astro.props;
---

<header {...attrs} class:list={[className]}>
  <slot />
</header>
