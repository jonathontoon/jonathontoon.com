---
import Text from "./Text.astro";

interface Props {
  class?: string;
}

const { class: className = "", ...attrs } = Astro.props;
---

<Text class={`text-xl font-bold leading-none ${className}`} {...attrs}>
  <slot />
</Text>
