---
import Text from './Text.astro';

interface Props {
  class?: string;
}

const { class: className = "", ...attrs } = Astro.props;
---

<Text class={`text-base md:text-base font-semibold ${className}`} {...attrs}>
  <slot />
</Text>