---
import Text from "./Text.astro";

interface Props {
  class?: string;
}

const { class: className = "", ...attrs } = Astro.props;
---

<Text class={`text-xl font-bold leading-none text-stone-700 dark:text-stone-50 ${className}`} {...attrs}>
  <slot />
</Text>
