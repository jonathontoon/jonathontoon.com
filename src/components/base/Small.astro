---
import type { HTMLAttributes } from "astro/types";

interface Props extends HTMLAttributes<"small"> {
  class?: string;
}

const { class: className = "", ...attrs } = Astro.props;
---

<small class:list={[className]} {...attrs}>
  <slot />
</small>
