---
import type { HTMLAttributes } from "astro/types";

interface Props extends HTMLAttributes<"details"> {
  class?: string;
}

const { class: className = "", ...attrs } = Astro.props;
---

<details class:list={[className]} {...attrs}>
  <slot />
</details>
