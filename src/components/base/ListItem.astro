---
import type { HTMLAttributes } from "astro/types";

interface Props extends HTMLAttributes<"li"> {
  class?: string;
}

const { class: className = "", ...attrs } = Astro.props;
---

<li class:list={[className]} {...attrs}>
  <slot />
</li>
