---
import type { HTMLAttributes } from "astro/types";

interface Props extends HTMLAttributes<"legend"> {
  class?: string;
}

const { class: className = "", ...attrs } = Astro.props;
---

<legend class:list={[className]} {...attrs}>
  <slot />
</legend>
