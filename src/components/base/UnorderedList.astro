---
import type { HTMLAttributes } from "astro/types";

interface Props extends HTMLAttributes<"ul"> {
  class?: string;
}

const { class: className = "", ...attrs } = Astro.props;
---

<ul class:list={[className]} {...attrs}>
  <slot />
</ul>
