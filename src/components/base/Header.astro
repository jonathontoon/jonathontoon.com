---
import type { HTMLAttributes } from "astro/types";

interface Props extends HTMLAttributes<"header"> {
  class?: string;
}

const { class: className = "", ...attrs } = Astro.props;
---

<header class:list={[className]} {...attrs}>
  <slot />
</header>
