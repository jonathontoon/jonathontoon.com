---
import type { HTMLAttributes } from "astro/types";

interface Props extends HTMLAttributes<"nav"> {
  class?: string;
}

const { class: className = "", ...attrs } = Astro.props;
---

<nav class:list={[className]} {...attrs}>
  <slot />
</nav>
