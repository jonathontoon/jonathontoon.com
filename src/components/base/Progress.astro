---
import type { HTMLAttributes } from "astro/types";

interface Props extends HTMLAttributes<"progress"> {
  class?: string;
}

const { class: className = "", ...attrs } = Astro.props;
---

<progress class:list={[className]} {...attrs}>
  <slot />
</progress>
