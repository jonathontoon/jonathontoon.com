---
import type { HTMLAttributes } from "astro/types";
import { cn } from "@utils/cn";

interface Props extends HTMLAttributes<"section"> {
  class?: string;
  id?: string;
}

const { class: className = "", ...attrs } = Astro.props;
---

<section class={cn(className)} {...attrs}>
  <slot />
</section>
