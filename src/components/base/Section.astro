---
import type { HTMLAttributes } from "astro/types";

interface Props extends HTMLAttributes<"section"> {
  class?: string;
  id?: string;
}

const { class: className = "", ...attrs } = Astro.props;
---

<section class:list={[className]} {...attrs}>
  <slot />
</section>
