---
import type { HTMLAttributes } from "astro/types";

interface Props extends HTMLAttributes<"td"> {
  class?: string;
}

const { class: className = "", ...attrs } = Astro.props;
---

<td class:list={["border-t border-zinc-100 px-6 py-4 text-sm", className]} {...attrs}>
  <slot />
</td>
