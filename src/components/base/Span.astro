---
import type { HTMLAttributes } from "astro/types";

interface Props extends HTMLAttributes<"span"> {
  class?: string;
}

const { class: className = "", ...attrs } = Astro.props;
---

<span class:list={[className]} {...attrs}>
  <slot />
</span>
