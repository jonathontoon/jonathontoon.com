---
import type { HTMLAttributes } from "astro/types";

interface Props extends HTMLAttributes<"svg"> {
  class?: string;
}

const { class: className = "", ...attrs } = Astro.props;
---

<svg class:list={[className]} {...attrs}>
  <slot />
</svg>
