---
import type { HTMLAttributes } from "astro/types";

interface Props extends HTMLAttributes<"h3"> {
  class?: string;
}

const { class: className = "", ...attrs } = Astro.props;
---

<h3 class:list={["font-primary leading-tight tracking-normal", className]} {...attrs}>
  <slot />
</h3>
