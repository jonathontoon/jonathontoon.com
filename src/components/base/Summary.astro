---
import type { HTMLAttributes } from "astro/types";

interface Props extends HTMLAttributes<"summary"> {
  class?: string;
}

const { class: className = "", ...attrs } = Astro.props;
---

<summary class:list={[className, "[&::-webkit-details-marker]:hidden"]} {...attrs}>
  <slot />
</summary>
