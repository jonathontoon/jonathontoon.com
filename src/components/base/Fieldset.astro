---
import type { HTMLAttributes } from "astro/types";
import { cn } from "@utils/cn";

interface Props extends HTMLAttributes<"fieldset"> {
  class?: string;
}

const { class: className = "", ...attrs } = Astro.props;
---

<fieldset class={cn(className)} {...attrs}>
  <slot />
</fieldset>
