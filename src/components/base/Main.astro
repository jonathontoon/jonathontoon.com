---
import type { HTMLAttributes } from "astro/types";

interface Props extends HTMLAttributes<"main"> {
  class?: string;
}

const { class: className = "", ...attrs } = Astro.props;
---

<main class:list={[className]} {...attrs}>
  <slot />
</main>
