---
import type { HTMLAttributes } from "astro/types";

interface Props extends HTMLAttributes<"footer"> {
  class?: string;
}

const { class: className = "", ...attrs } = Astro.props;
---

<footer class:list={[className]} {...attrs}>
  <slot />
</footer>
